# Study Log v1.0.0 Release Notes

## 版本信息

- 项目名称：Study Log
- 版本：v1.0.0
- 状态：Stable（首个正式版本）
- 发布日期：2026-08-01

## 项目简介

Study Log 是一个围绕“上传学习成果”设计的个人学习记录网站。每天完成学习后拍摄并上传作业照片，网站负责保存、整理和统计学习记录，帮助养成持续学习的习惯。本项目为纯静态前端实现，无后端服务。

## 已实现功能

### 首页 Dashboard

- 显示今日日期与星期
- 显示当前学习计划入口
- 显示 6 个学科的“已上传 / 未上传”状态
- 显示今日上传统计数量、百分比与进度条
- 汇总今日各学科学习记录
- 显示 LocalStorage 数据占用，接近上限时提示导出备份

### 学科页面

- 统一学科模板，支持数学、英语、语文、物理、化学、历史
- 今日作业文本输入
- JPG / PNG 多图上传，自动压缩后保存
- 图片预览、单张移除、点击查看大图
- 自动记录保存时间
- 按日期展示历史记录，每页 10 条，支持“加载更多”
- 支持拖拽图片上传和 `Ctrl/Cmd + V` 粘贴截图
- 源图片超过 15MB 时提示

### 学习日志

- 默认查看今天的记录
- 支持前一天 / 后一天切换和日期选择器
- 支持从学习日历点击日期直接跳转
- 汇总当天有记录的学科数与已上传图片的学科数
- 展示作业文本、上传时间和图片缩略图
- 点击图片查看大图

### 学习计划

- 支持上传 PDF、JPG、PNG
- 支持编辑计划名称
- 支持查看、替换、移除学习计划
- 图片计划直接预览，PDF 计划以内嵌预览显示
- PDF 单文件限制 4MB
- 支持拖拽文件上传
- 首页同步显示当前计划名称与类型

### 数据统计

- 总上传图片数量
- 连续上传天数（截至最近有记录的一天）
- 本月上传图片数量与本月记录条数
- 各学科上传次数与图片数量图表

### 学习日历

- 月视图，周一起始
- 有上传记录的日期显示标记
- 今日高亮
- 支持上一月 / 下一月 / 回到今天
- 点击日期跳转至对应学习日志

### 数据与备份

- 使用 LocalStorage 持久化保存学习记录、学习计划和设置
- 导出 `studylog.json`，保留全部记录、计划和设置
- 导入支持合并与覆盖两种模式
- 自动过滤损坏记录，非法 JSON 导入给出提示
- 存储容量不足时给出中文提示

## 技术实现

- 纯静态多页面：HTML + CSS + JavaScript
- 无框架、无构建工具、无后端
- 使用经典 `<script>` 加载方式，直接双击 `index.html` 即可运行
- 所有数据访问统一封装在 `storage.js`，页面不直接操作 `localStorage`
- 记录以 `date + subject` 为唯一键，包含 `id`、`createdAt`、`updatedAt` 元数据
- 统计和日历数据由记录实时计算，不保存冗余统计结果
- 响应式布局，支持桌面、平板与手机
- CSS 设计变量统一管理颜色、间距、字号、圆角和阴影

## 重要技术决策

- 第一版采用纯前端 + LocalStorage，快速交付且无需服务器，为后续云同步保留替换空间
- 图片先通过 Canvas 压缩（最长边 1600px，JPEG 质量 0.82）再入库，平衡清晰度与存储占用
- PDF 计划使用 Blob + ObjectURL 预览，规避部分浏览器对 data URL 内嵌 PDF 的限制
- 导入默认合并、可选覆盖，避免误操作丢失现有数据
- 数据版本号 `study_log_version` 为后续数据迁移预留入口
- 学科配置集中在 `subjects.js`，方便后续扩展

## 运行方式

```bash
# 直接使用
open index.html

# 或本地静态服务
python3 -m http.server

# 运行自动化测试（需要 Node.js）
node tests/run-tests.js
```

部署时将整个目录上传到任意静态托管服务即可，无特殊配置。

## 测试结果

- 自动化测试：38 项全部通过
- 覆盖范围：LocalStorage 增删改查、导入导出、数据损坏过滤、容量提示、统计计算、日历生成、图片与计划文件类型校验
- JavaScript 语法检查：全部通过
- 说明：受本机沙盒限制，未完成多浏览器自动实测，跨浏览器体验需人工验证

## 已知限制

- 数据保存在浏览器 LocalStorage，容量有限，长期使用建议定期导出备份
- 数据绑定当前浏览器，更换设备或清除浏览器数据后需通过 `studylog.json` 恢复
- 第一版固定 6 个学科
- PDF 计划单文件上限 4MB，学科图片源文件上限 15MB
- AI OCR、AI 学习总结、云同步、用户登录未实现，属于后续 Roadmap

## v1.1 方向

- 以真实使用反馈为准进行修复与体验优化
- 不预先承诺新功能，优先保持稳定性和数据安全
